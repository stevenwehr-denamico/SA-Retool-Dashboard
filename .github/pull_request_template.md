## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] `denamico-sa-toolkit.jsx` and `denamico-sa-toolkit-web/src/App.jsx` are in sync if both were meant to change (or only one was intentionally updated).
- [ ] `CHANGELOG.md` updated under `[Unreleased]` (or version section) when the change is user- or team-visible.
- [ ] `npm run build` succeeds in `denamico-sa-toolkit-web/` when web assets or `App.jsx` changed.

## Notes

<!-- Optional: links to tickets, screenshots, rollout steps -->
